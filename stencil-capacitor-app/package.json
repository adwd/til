{
  "name": "stencil-capacitor-app",
  "private": true,
  "version": "0.0.1",
  "description": "Stencil App Starter",
  "scripts": {
    "build": "stencil build",
    "start": "stencil build --dev --watch --serve",
    "test": "stencil test --spec --e2e",
    "test.watch": "stencil test --spec --e2e --watchAll",
    "open.ios": "npm run build && npx cap copy && npx cap open ios"
  },
  "dependencies": {
    "@capacitor/cli": "^1.0.0",
    "@capacitor/core": "^1.0.0",
    "@capacitor/ios": "^1.0.0",
    "@stencil/core": "1.0.1",
    "@stencil/router": "^1.0.0"
  },
  "license": "MIT"
}
