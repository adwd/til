type Story {
  by: String!
  descendants: Int!
  id: Int!
  score: Int!
  time: Int!
  title: String!
  type: String!
  url: String!
  ogpImage: String
  kids: [Int!]!
}

type Query {
  stories(limit: Int): [Story!]!
  story(id: ID!): Story
}
